import axios from 'axios';

async function test() {
  try {
    // 平良 (Seibu, apiTeamId=9), 万波 (Fighters, apiTeamId=8)
    // Actually, Taira API ID is 246? We don't know exactly unless we check mockData.js.
    // Let's just check the data.
  } catch (e) {
    console.error(e);
  }
}
